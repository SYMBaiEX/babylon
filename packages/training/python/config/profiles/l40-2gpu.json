{
  "name": "2x L40 GPUs (96GB total)",
  "description": "Dual L40 with vLLM tensor parallel across both GPUs (for larger 14B+ models)",
  "model": "Qwen/Qwen2.5-14B-Instruct",
  "vllm_gpu_memory": 0.45,
  "vllm_gpu": "0,1",
  "training_gpu": "0",
  "tensor_parallel_size": 2,
  "batch_size": 4,
  "max_token_length": 8192,
  "group_size": 4,
  "total_steps": 8000,
  "steps_per_eval": 400,
  "gradient_accumulation_steps": 4,
  "learning_rate": 8e-6,
  "warmup_steps": 150,
  "kl_coefficient": 0.05,
  "use_flash_attention": true,
  "notes": "vLLM uses both GPUs via tensor_parallel (14B needs ~40GB). Training shares GPU 0 - careful memory allocation!"
}



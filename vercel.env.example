# Vercel Environment Variables Template
# Copy these to your Vercel Project Settings → Environment Variables

# ============================================
# DATABASE (Required)
# ============================================
DATABASE_URL=postgresql://user:password@host:5432/babylon

# ============================================
# REDIS (Recommended for Real-time Features)
# ============================================
# Option 1: External Redis (Upstash, Redis Cloud, etc.)
# Use this for Vercel production deployments
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here

# Option 2: Standard Redis URL (local development)
# Automatically set by pre-dev script to port 6380 (to avoid conflicts)
# REDIS_URL=redis://localhost:6380

# Note: Without Redis, SSE will work locally but not across Vercel instances.
# Real-time features will fall back to polling (15-30s delays).

# ============================================
# AUTHENTICATION (Required)
# ============================================
PRIVY_APP_ID=your_privy_app_id
PRIVY_APP_SECRET=your_privy_secret
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id

# ============================================
# GAME ENGINE (Required for Content Generation)
# ============================================
# LLM API - Use Groq (faster) or OpenAI
GROQ_API_KEY=gsk_your_groq_api_key
# OR
OPENAI_API_KEY=sk-your_openai_key

# Cron job authentication (generate with: openssl rand -hex 32)
CRON_SECRET=your_random_secret_here

# ============================================
# OPTIONAL: Admin & Testing
# ============================================
ADMIN_TOKEN=your_admin_token

# ============================================
# BLOCKCHAIN (Ethereum Sepolia - Unified with Agent0)
# ============================================
# Ethereum Sepolia RPC URL (same chain as Agent0 registry)
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
NEXT_PUBLIC_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
NEXT_PUBLIC_CHAIN_ID=11155111

# Contract addresses (set after deployment)
NEXT_PUBLIC_IDENTITY_REGISTRY_SEPOLIA=0x...
NEXT_PUBLIC_REPUTATION_SYSTEM_SEPOLIA=0x...
NEXT_PUBLIC_DIAMOND_SEPOLIA=0x...

# Etherscan API key for contract verification
ETHERSCAN_API_KEY=your_etherscan_api_key

# ============================================
# OPTIONAL: Image Generation
# ============================================
FAL_KEY=your_fal_api_key

# ============================================
# OPTIONAL: Static Assets CDN
# ============================================
# If you want to serve static assets from a custom CDN, set this URL
# Otherwise, assets will be served from the default Next.js path
# NEXT_PUBLIC_STATIC_ASSETS_URL=https://cdn.yourdomain.com


# Babylon RL Training - Multi-Backend Support
# Supports: CUDA (GPU), MLX (Apple Silicon), CPU, and W&B Serverless

# Core ART framework (for CUDA/Serverless backends)
openpipe-art>=0.5.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# HTTP client
httpx>=0.26.0

# OpenAI client (for inference)
openai>=1.0.0

# Monitoring (W&B is included with openpipe-art)
# wandb is a dependency of openpipe-art

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0
pydantic>=2.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Utilities
tqdm>=4.66.0
psutil>=5.9.0  # For resource checking

# ============================================
# OPTIONAL: MLX Backend (Apple Silicon only)
# ============================================
# Install on Mac with Apple Silicon:
#   pip install mlx mlx-lm
#
# For fine-tuning support:
#   pip install mlx-lm[finetuning]
#
# Recommended models for MLX:
#   - mlx-community/Qwen2.5-3B-Instruct-4bit
#   - mlx-community/Qwen2.5-7B-Instruct-4bit
#   - mlx-community/Qwen3-4B-4bit

# ============================================
# OPTIONAL: CPU Backend (transformers/peft)
# ============================================
# Install for CPU training (SLOW):
#   pip install transformers peft torch
#
# Recommended models for CPU:
#   - unsloth/Qwen3-4B-128K (with reduced batch size)
